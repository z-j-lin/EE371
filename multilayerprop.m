%get user inputs 
n = input("Enter the number of layers");
%get parameters of each region 
%conductivity 
c = zeros(n);
%permitivity 
e = zeros(n);
%permeability
u = zeros(n);
%for loop to get the input data
